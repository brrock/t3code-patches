# 01-opencode-show-provider-composer

Refresh of upstream PR `pingdotgg/t3code#2171` against current `main`.

Intent:

- Hide the OpenCode sub-provider prefix in the composer model picker trigger by default.
- Add an OpenCode provider setting to opt back into showing the upstream provider name.
- Keep the model list/search behavior unchanged outside the trigger display.
